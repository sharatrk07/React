store: stores the data
reducers: helps to make any modifications in the store using actions
action()
slice()
useSelector(): Used to fetch the data from store
useDispatcher(): Used to send the data into the store

state, actions, reducers, store, slices, and middleware

configureStore - 
createSlice - 
createAsyncThunk - 
createSelector - 
createEntityAdapter - # React
